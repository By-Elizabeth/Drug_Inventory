server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/medicamentos_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

app:
  jwt:
    secret: "CAMBIA_ESTE_SECRETO_SUPER_LARGO_DE_32_CHARS_MINIMO_123456"
    expirationMinutes: 480

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
